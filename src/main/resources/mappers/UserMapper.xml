<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.car_parking_backend_api.mapper.UserMapper">

    <resultMap id="spot" type="com.example.car_parking_backend_api.model.User">
        <result property="id" column="id" />
        <result property="name" column="first_name" />
        <result property="email" column="email" />
        <result property="password" column="password" />
        <result property="role" column="role_name" />
    </resultMap>

    <select id="findByEmail" resultMap="spot">
        SELECT
            u.id,
            u.first_name,
            u.email,
            u.password,
            r.name
        FROM user u
        JOIN user_role ur ON u.id = ur.user_id
        JOIN role r ON ur.role_id = r.id
        WHERE u.email = #{email}
    </select>
</mapper>